Инструкция для администратора (подробно)

Главное меню админа
- «📊 Отчёты» — выгрузка XLSX (полный/рабочий).
- «🛠 Изменить статус» — массовое обновление статусов по XLSX.
- «❓ Вопрос пользователю» — отправить вопрос по конкретной заявке.
- «📣 Push-рассылка» — разослать текст выбранным пользователям.
- «⚙️ Админ-настройки» — админы, макросы, словарь видов.
- «📈 Аналитика» — базовая статистика.
- Также доступны обычные пользовательские кнопки («📝 Оставить заявку», «📋 Мои заявки», «ℹ️ Как это работает»).

Отчёты («📊 Отчёты»)
1) Нажмите кнопку → выберите:
   - «📄 Полный отчёт» — все заявки.
   - «🗂 Рабочий отчёт» — без финальных статусов.
2) Получаете XLSX. Колонки «Статус» и «Вид» имеют выпадающий список (скрытые листы). Строки подсвечиваются по статусу. «Вид» распознаётся по словарю (kind_keywords), можно менять вручную.

Массовое обновление статусов («🛠 Изменить статус»)
1) Нажмите кнопку → бот просит загрузить .xlsx. Обязательные колонки: «ID заказа», «Статус». Можно добавить «Ссылка на товар».
2) Загрузите файл документом.
3) После обработки обновятся записи в БД, попадёт в history (order_status_logs), пользователи получат уведомления по затронутым заявкам.

Вопрос пользователю («❓ Вопрос пользователю»)
1) Бот просит ID заявки (из таблицы). Введите число.
2) Бот найдёт заявку и предложит:
   - Выбрать макрос (список «#id · название») — отправится текст макроса.
   - «✏️ Свой вопрос» — ввести текст вручную.
   - «⬅️ Ввести другой ID» — сменить заявку.
   - «🏠 Главное меню» — выйти.
3) После выбора/ввода вопрос отправляется пользователю, заявка получает статус «Нужно уточнение».
4) Пользователь отвечает → видит превью своего ответа → подтверждает «✅ Отправить ответ» или «✏️ Исправить». После подтверждения статус «Ответ получен», админ получает ответ.

Push-рассылка («📣 Push-рассылка»)
1) Введите ID пользователей через запятую (пример: 123,456).
2) Введите текст рассылки.
3) Бот покажет предпросмотр (ID + текст) с кнопками:
   - «📤 Отправить»
   - «✏️ Изменить текст»
   - «✖️ Отмена»
4) После отправки бот сообщит об успехах/ошибках.

Админ-настройки («⚙️ Админ-настройки»)
- «👤 Администраторы»
  - «➕ Добавить» — введите ID, чтобы назначить админом.
  - «➖ Удалить» — введите ID, чтобы снять права.
  - «⬅️ Назад», «🏠 Главное меню».
- «📝 Макросы»
  - Список макросов (#id · заголовок) → открыть/редактировать/удалить.
  - «➕ Создать макрос» — поэтапно: заголовок → текст → предпросмотр → «💾 Сохранить».
  - Управление: «✏️ Заголовок», «📝 Текст», «⬅️ К списку», «🏠 Главное меню».
- «🗂 Вид» (распознавание товара)
  - Список видов: Одежда, Обувь, Инвентарь, Аксессуары.
  - Карточка вида: список слов (ключевых подстрок), кнопки «➕ Добавить слово», «➖ Удалить слово», «⬅️ Назад», «🏠 Главное меню».
  - Слова сохраняются в lowercase, не могут повторяться в разных видах (валидация при добавлении).

Аналитика («📈 Аналитика»)
- Показывает: всего заявок, за 7 дней, уникальные пользователи, распределение по статусам, топ брендов (3).
- Кнопка «🔄 Обновить» обновит блок; «🏠 Главное меню» вернёт к началу.

Работа с карточкой заявки (как у пользователя, но с учётом прав)
- «📋 Мои заявки» доступно и админам: список с кнопками номеров (кнопки — короткий номер, текст — {public_id}-{user_order_number}).
- В карточке: «✏️ Изменить заявку» / «🗑 Удалить заявку» (если статус не финальный), «⬅️ К моим заявкам», «🏠 Главное меню».
- Редактирование: выбор поля → ввод → предпросмотр → «✅ Отправить» подтверждает изменения.

Статусы и переходы (основные)
- Новая заявка → В обработке (системно/оператором).
- В обработке → Нужно уточнение (при вопросе пользователю).
- Нужно уточнение → Ответ получен (после подтверждённого ответа).
- В обработке/Ответ получен → Добавлен / Не будет добавлен (финал).
- Отменена пользователем — финал после «🗑 Удалить заявку».

Нумерация
- Пользовательский номер: {public_id}-{user_order_number} — показывается в тексте. На кнопках — только user_order_number (для компактности).
